apiVersion: v1
kind: Service
metadata:
  name: yt-downloader-service
spec:
  type: NodePort
  sessionAffinity: ClientIP
  selector:
    app: yt-downloader
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30501